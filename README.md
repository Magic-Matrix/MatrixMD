

### install

```shell
sudo apt install cmake
sudo apt install make
sudo apt install g++
sudo apt install gcc
```


### build

```shell
mkdir build
cd build
cmake ..
make
```

### run

```shell
./build/matrix_md
```